<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="20dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/post_title_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="22sp"
        android:paddingBottom="10dp"
        android:textStyle="bold"
        android:hint="Title and Username"
        android:textColor="@color/blue" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/post_title_text"
        android:src="@mipmap/ic_launcher_round"
        android:id="@+id/options_photo1"/>
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/options_photo1"
        android:src="@mipmap/ic_launcher_round"
        android:id="@+id/options_photo2"/>
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:src="@mipmap/ic_launcher_round"
        android:layout_below="@+id/options_photo2"
        android:id="@+id/options_photo3"/>

    <Button
        android:id="@+id/cast_vote_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/light_blue"
        android:gravity="center"
        android:layout_below="@+id/options_photo3"
        android:text="Cast Vote!"/>

</RelativeLayout>